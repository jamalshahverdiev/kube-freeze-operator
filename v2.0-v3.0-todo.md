# TODO — kube-freeze-operator v2.0 and v3.0

> This document complements the v1.0 plan and defines the **exact scope of work** for v2.0 and v3.0.
> Format: tasks, subtasks, acceptance criteria, tests, documentation.
> Principle: clear specification first → implementation second.

---

## v2.0 — GitOps-friendly Pause/Resume + Noise Reduction

### Goal of v2.0
Make it so that during active freeze:
- GitOps (Argo CD / Flux) **doesn't try to sync indefinitely** and doesn't clutter the UI with errors,
- operator can **put GitOps into "pause"** and restore it back,
- while webhook enforcement from v1.0 remains the source of truth (and protection).

---

## 1) Scope & decisions (mandatory decisions before code)

- [x] Choose Argo CD integration strategy
  - [x] Option A: manage **ArgoCD Application CR** (recommended)
  - [x] Option B: ArgoCD API calls (undesirable in v2.0)
- [x] Choose Flux integration strategy
  - [x] Manage **Kustomization** and/or **HelmRelease** resources via their suspend fields
- [x] Define behavior model:
  - [x] During freeze: put GitOps in pause (if enabled)
  - [x] Outside freeze: restore GitOps (only if operator touched it)
- [x] Define conflict policy:
  - [x] If user manually changed paused/suspend → who "wins"?
  - [x] Recommendation: **don't overwrite** manual changes; restore only what's managed by operator.
- [ ] Update v2.0 specification (docs/spec.md)
  - [ ] Define policy fields for GitOps behavior
  - [ ] Define policy priorities during GitOps pause

**Definition of Done**
- Decisions on ArgoCD/Flux approaches are made and documented in docs/spec.md

---

## 2) API changes (CRD extensions)

### 2.1 Add GitOps section to policies
- [x] MaintenanceWindow.spec.behavior.gitops (new block)
  - [x] `enabled: bool`
  - [x] `providers: ["argocd","flux"]` (list)
  - [x] `argocd`:
    - [x] `applicationSelector` (label selector)
    - [x] `namespaceSelector` (for namespace where Applications are located)
    - [ ] `pauseMode` (possible modes):
      - [x] `DisableAutoSync` (if Application managed, disable automated)
      - [ ] `SetAppPausedAnnotation` (if using your annotation/plugin mechanism)
  - [x] `flux`:
    - [x] `kustomizationSelector` / `helmReleaseSelector`
    - [x] `namespaceSelector`
- [x] ChangeFreeze.spec.behavior.gitops (similarly)
- [x] FreezeException — decide: should GitOps pause be allowed? (usually not)

### 2.2 Status improvements (visibility)
- [x] MaintenanceWindow.status:
  - [x] `gitopsPausedCount` (how many objects in pause)
  - [x] `gitopsLastReconcileTime`
- [x] ChangeFreeze.status:
  - [x] similarly

### 2.3 Schema validations
- [x] If `behavior.gitops.enabled=true`:
  - [x] require valid selectors
  - [x] restrict allowed provider values

**Definition of Done**
- CRD schemas are updated, sample YAMLs exist in `/config/samples/` and `/examples/`

---

## 3) Argo CD integration (controller)

### 3.1 Research and management model
- [x] Define "pause" method for ArgoCD Application
  - [x] Method 1 (recommended): patch `spec.syncPolicy.automated` (on/off)
  - [x] Additionally: optionally set annotation `freeze-operator.io/argocd-paused="true"`
- [x] Choose safe "restore" method
  - [x] Store original state:
    - [x] managed annotation `freeze-operator.io/managed="true"`
    - [x] `freeze-operator.io/original-autosync="true|false"`
    - [x] `freeze-operator.io/managed-by-policy="<policyRef>"`

### 3.2 Implement reconciler
- [x] Watch ArgoCD Application CR (if CRD installed)
- [x] For each target Application:
  - [x] Calculate active freeze state (via policy engine)
  - [x] If freeze active and gitops enabled:
    - [x] If autosync enabled → disable + mark managed + save original
  - [x] If freeze not active:
    - [x] If managed by operator → restore original autosync value and remove managed annotations
- [x] Add conflict protection:
  - [x] if original not found → don't touch (fail-safe)
  - [x] if policy changed → recalculate desired

### 3.3 RBAC
- [x] Add RBAC for read/patch ArgoCD Application (usually group `argoproj.io`)
- [ ] Make this optional via helm values (if argo missing — don't require permissions)

### 3.4 Compatibility
- [ ] Presence/absence of ArgoCD CRD:
  - [ ] if CRD missing, controller doesn't start / disables gracefully

**Definition of Done**
- In production scenario ArgoCD stops spamming sync errors during freeze, everything restores after

---

## 4) Flux integration (controller)

### 4.1 Choose resources (minimum)
- [x] Flux Kustomization (kustomize.toolkit.fluxcd.io)
- [x] Flux HelmRelease (helm.toolkit.fluxcd.io)

### 4.2 Implement pause/resume
- [x] For Kustomization:
  - [x] manage `spec.suspend`
- [x] For HelmRelease:
  - [x] manage `spec.suspend`
- [x] Managed annotations:
  - [x] `freeze-operator.io/managed="true"`
  - [x] `freeze-operator.io/original-suspend="true|false"`
  - [x] `freeze-operator.io/managed-by-policy="<policyRef>"`

### 4.3 RBAC
- [x] RBAC for patch Kustomization/HelmRelease
- [ ] Optional enabling via values

### 4.4 Missing CRD
- [ ] graceful disable if CRD not installed

**Definition of Done**
- Flux stops applying changes during freeze and correctly resumes after

---

## 5) Webhook noise reduction (UX improvements)

- [x] Improve deny message for GitOps:
  - [x] recognize user-agent / serviceaccount Argo/Flux
  - [x] provide short clear message (without extra text)
- [ ] Add rate limiting / sampling for Events (to avoid flooding cluster events)
- [x] Add correlation fields in logs:
  - [x] request UID
  - [x] policyRef
  - [x] gitops provider detected

**Definition of Done**
- During active freeze, GitOps UI is cleaner, not too many events, messages are clear

---

## 6) Testing (v2.0)

### Unit tests
- [x] Policy engine: gitops behavior flags don't break basic logic
- [x] Managed restore logic for Argo/Flux (table test cases)

### Integration tests (envtest / kind)
- [x] With ArgoCD CRDs installed:
  - [x] autosync toggled on freeze, restored after
- [x] With Flux CRDs installed:
  - [x] suspend toggled on freeze, restored after
- [x] Webhook still denies direct workload changes

**Definition of Done**
- CI runs tests; e2e scenarios confirm pause/resume

---

## 7) Documentation (v2.0)

- [x] docs/gitops-argocd.md
  - [x] how to enable in values
  - [x] how to target apps selectors
  - [x] what restore looks like
- [x] docs/gitops-flux.md
- [x] docs/upgrade-v1.0-to-v2.0.md
- [x] examples:
  - [x] prod window + argocd pause
  - [x] changefreeze + flux pause

**Release checklist v2.0**
- [ ] v2.0.0 tag + release notes
- [ ] chart updated (GitOps options, RBAC optional)
- [x] backward compatible with v1.0 policies

---

---

# v3.0 — CI Helper API + Ready-to-use CI Templates

### Goal of v3.0
Provide an easy way to integrate freeze into CI/CD:
- REST API: "can I deploy now?"
- standardized templates for GitLab CI / GitHub Actions
- unified response format (to easily abort job with explanation)
- (optional) CLI utility `kfo` for local checking.

---

## 1) Scope & design decisions (before code)

- [x] Choose interface:
  - [x] HTTP REST endpoint (inside cluster) + optional Ingress
- [x] Choose authentication:
  - [x] v3.0 MVP: Kubernetes ServiceAccount token (in-cluster) + RBAC "can-i"
  - [x] Out-of-cluster: kubeconfig/port-forward (doc)
- [x] Define endpoint contract:
  - [x] `GET /v1/evaluate?namespace=...&kind=...&action=...`
  - [x] `POST /v1/evaluate` (for complex payload)
- [x] Define request fields:
  - [x] namespace
  - [x] kind
  - [x] name (optional)
  - [x] action (ROLL_OUT/SCALE/CREATE/DELETE)
- [x] Define response fields:
  - [x] allow (bool)
  - [x] reason (string)
  - [x] matchedPolicy (ref)
  - [x] nextAllowedTime / freezeEndTime
  - [x] policyKind
- [x] Define API stability:
  - [x] v3.0: `v1` endpoint, documented in docs/ci-api.md

**Definition of Done**
- API contract described in docs/api.md + request/response examples

---

## 2) API Server component

- [x] Add HTTP server to manager (in-process, `mgr.Add(Runnable)`)
- [x] Implement handler:
  - [x] normalize kind/action (with validation)
  - [x] call policy engine via `policy.Evaluator.Evaluate()`
- [x] Logging and metrics:
  - [x] `freeze_operator_api_requests_total{decision,namespace,kind,action}`
  - [x] `freeze_operator_api_latency_seconds` histogram
  - [x] `freeze_operator_api_errors_total{error_type}`
- [x] Security:
  - [x] enable/disable API via `--api-bind-address` flag (set to `0` to disable)
  - [x] restrict access with NetworkPolicy (docs/upgrade-v2.0-to-v3.0.md)

**Definition of Done**
- API responds correctly and predictably, doesn't require excessive permissions in mode A

---

## 3) CI templates (ready to copy-paste)

### 3.1 GitLab CI template
- [x] Create `/ci/gitlab/freeze-check.yml`
  - [x] job/stage `.freeze-check`
  - [x] variables: FREEZE_API_URL, FREEZE_NAMESPACE, FREEZE_KIND, FREEZE_ACTION, FREEZE_FAIL_ON_ERROR
  - [x] script: curl → parse json → if deny → fail job with reason
- [x] Add docs:
  - [x] how to include in `.gitlab-ci.yml` (docs/ci-api.md)
  - [x] how to pass cluster access

### 3.2 GitHub Actions (optional but nice)
- [ ] Workflow snippet
- [ ] example using kubectl port-forward + curl

**Definition of Done**
- User can connect template in 5–10 minutes and get deployment blocking in CI

---

## 4) Optional: CLI `kfo` (if time permits in v3.0)

- [x] Command `kfo can-i`:
  - [x] parameters: namespace/kind/name/action
  - [x] modes:
    - [x] API call (--api-url)
    - [x] direct kube API (kubeconfig, default)
- [x] Output:
  - [x] human-friendly + `--json` for automation

**Definition of Done**

- CLI works as convenient helper, but not mandatory for v3.0 release

---

## 5) Testing (v3.0)

- [x] Unit tests:
  - [x] API contract (validators, required fields): 9 test cases
  - [x] policy evaluation via API (allow, deny, exception)
- [x] Integration tests:
  - [x] `hack/v3-integration-test.sh` — 11 checks (healthz, allow, deny, GET, bad request, metrics)
  - [x] Manual live test passed (5 curl tests in cluster)
- [x] CI template smoke test (doc-level):
  - [x] example in docs/ci-api.md

**Definition of Done**
- API and templates are covered by tests, main cases don't break with changes

---

## 6) Documentation (v3.0)

- [x] docs/ci-api.md
  - [x] endpoints, request/response examples
  - [x] accessing API (Service, port-forward)
  - [x] GitLab CI integration guide
- [ ] docs/ci/github.md (optional, deferred to v3.1)
- [x] docs/upgrade-v2.0-to-v3.0.md
- [x] examples:
  - [x] GitLab CI pipeline sample in docs/ci-api.md

**Release checklist v3.0**
- [x] v3.0.0 tag + release notes
- [x] CI templates included and documented
- [x] Backward compatible with v1.0/v2.0
- [x] Helm chart: api.enable + api.authMode (none/token) + RBAC for TokenReview

**v3.0.1 — Token Authentication**
- [x] `--api-auth-mode=token` flag with Kubernetes TokenReview
- [x] Bearer token middleware (healthz bypass)
- [x] 5 unit tests (no token, invalid, valid, healthz bypass, extractBearerToken)
- [x] RBAC: ClusterRole/Binding for tokenreviews (config/rbac + Helm)
- [x] Helm chart: `api.authMode` value + conditional RBAC template

---

## Roadmap Notes (after v3.0)
- v4.0: Multi-cluster policies + global freeze propagation
- v5.0: FreezeEvent CRD + external audit sinks (Webhook → ELK/OTel)
- v6.0: Advanced rule matching (field selectors/diff constraints)
