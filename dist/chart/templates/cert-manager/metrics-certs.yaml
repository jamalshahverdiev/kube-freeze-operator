{{- if and .Values.certManager.enable .Values.metrics.enable }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "kube-freeze-operator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "kube-freeze-operator.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
    namespace: {{ .Release.Namespace }}
spec:
    dnsNames:
        - {{ include "kube-freeze-operator.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" .) }}.{{ include "kube-freeze-operator.namespaceName" . }}.svc
        - {{ include "kube-freeze-operator.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" .) }}.{{ include "kube-freeze-operator.namespaceName" . }}.svc.cluster.local
    issuerRef:
        kind: Issuer
        name: {{ include "kube-freeze-operator.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
    secretName: metrics-server-cert
{{- end }}
